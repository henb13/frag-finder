export const CSGO_ROUND_LENGTH = 115;

// A healthy amount of tick before the first frag, so when going to the first frag in the demo, one has a couple of seconds of breathing space before it actually happens
export const TICK_BUFFER = 200;
